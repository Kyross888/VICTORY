[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqlnd", "php82Extensions.curl", "php82Extensions.mbstring", "php82Extensions.session", "apache"]

[phases.build]
cmds = []

[start]
cmd = "apache2ctl -D FOREGROUND"
